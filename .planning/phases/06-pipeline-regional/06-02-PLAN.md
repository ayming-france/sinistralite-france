---
phase: 06-pipeline-regional
plan: 02
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - data/pipeline/refresh_data.py
  - data/pipeline/parse_regional.py
  - data/regional-data.json
autonomous: false
requirements:
  - PIPE-05
  - PIPE-06
  - PIPE-07
  - PIPE-08

must_haves:
  truths:
    - "Running parse_regional.py with the real PDF produces a valid regional-data.json"
    - "regional-data.json contains at least 16 metropolitan caisses with AT and Trajet data for 2020-2024"
    - "refresh_data.py accepts --rapport-pdf and calls parse_regional when provided"
    - "Caisse names in the JSON match canonical names from the PDF without truncation"
    - "No Total/Ensemble/France rows appear in the output"
  artifacts:
    - path: "data/regional-data.json"
      provides: "Regional AT/Trajet data for frontend consumption"
      contains: "caisses"
    - path: "data/pipeline/refresh_data.py"
      provides: "Updated pipeline with --rapport-pdf integration"
      contains: "rapport-pdf"
  key_links:
    - from: "data/pipeline/refresh_data.py"
      to: "data/pipeline/parse_regional.py"
      via: "import parse_regional_pdf"
      pattern: "from.*parse_regional.*import"
    - from: "data/pipeline/parse_regional.py"
      to: "data/regional-data.json"
      via: "json.dump file write"
      pattern: "json\\.dump"
---

<objective>
Run parse_regional.py against the real rapport annuel PDF, fix any extraction issues, integrate into refresh_data.py, and produce the final regional-data.json.

Purpose: Validate the parser against real data, fix edge cases, and wire the regional pipeline into the existing refresh workflow.
Output: `data/regional-data.json` with real data, `refresh_data.py` updated with `--rapport-pdf` support
</objective>

<execution_context>
@/Users/encarv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/encarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-pipeline-regional/06-RESEARCH.md
@.planning/phases/06-pipeline-regional/06-01-SUMMARY.md
@data/pipeline/parse_regional.py
@data/pipeline/refresh_data.py
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User provides rapport annuel PDF path</name>
  <files>none</files>
  <action>
    The rapport annuel PDF from ameli.fr is required to test the parser.
    This PDF is not in the repository and must be downloaded manually.

    1. Download the rapport annuel from: https://assurance-maladie.ameli.fr (section "Risques professionnels", rapport annuel)
    2. Provide the local file path to the PDF

    If you already have the PDF, provide its path. Example: `/Users/encarv/Downloads/rapport-annuel-2024.pdf`
  </action>
  <verify>User provides a valid file path to an existing PDF file</verify>
  <done>PDF path is known and file exists on disk</done>
</task>

<task type="auto">
  <name>Task 2: Run parser against real PDF and fix extraction issues</name>
  <files>data/pipeline/parse_regional.py, data/regional-data.json</files>
  <action>
Using the PDF path provided by the user in Task 1:

1. **Dry run first**: Execute `python data/pipeline/parse_regional.py --pdf {PDF_PATH} --dry-run` to see all extracted caisse names. Check for:
   - Truncated names (ending in "-")
   - Duplicate names (multi-line merge failure)
   - Unrecognized names (not in CAISSE_MAP)
   - Missing caisses (fewer than 16 metro)

2. **Fix CAISSE_MAP if needed**: If any canonical name in the PDF differs from what's in CAISSE_MAP (spacing, accents, abbreviation), update the map keys to match the actual PDF text. Do NOT change the ID values.

3. **Fix column detection if needed**: If numeric columns are misaligned (wrong year mapped to wrong value), adjust the header row detection logic or column indices in `extract_regional_table()`.

4. **Full run**: Execute `python data/pipeline/parse_regional.py --pdf {PDF_PATH} --out data/regional-data.json`. Verify:
   - Exit code 0
   - Output reports 16+ metropolitan caisses
   - No AssertionError from validate_output

5. **Validate JSON content**:
   - Check `data/regional-data.json` has the correct schema (meta, caisses array)
   - Verify AT counts are plausible (thousands, not millions or single digits)
   - Verify Trajet counts are present and smaller than AT counts (expected pattern)
   - Verify salaries field is present for metropolitan caisses
   - Verify no "Total" or "Ensemble" entries exist in the caisses array

6. **Update unit tests if needed**: If CAISSE_MAP keys were adjusted, update `test_parse_regional.py` to match. Re-run all tests to confirm they pass.
  </action>
  <verify>
    <automated>cd /Users/encarv/.claude/datagouv && python -c "import json; d=json.load(open('data/regional-data.json')); metro=[c for c in d['caisses'] if c['type'] in ('carsat','cramif')]; assert len(metro)>=16, f'Only {len(metro)} metro caisses'; assert all('at' in c and 'trajet' in c for c in metro), 'Missing AT or Trajet'; assert all(c['at'].get('2023',0)>0 for c in metro), 'Zero AT 2023'; print(f'OK: {len(d[\"caisses\"])} caisses, {len(metro)} metro')"</automated>
    <manual>Spot-check 2-3 caisse values against the PDF to confirm accuracy</manual>
  </verify>
  <done>regional-data.json exists with 16+ metropolitan caisses, all having non-zero AT and Trajet for 2020-2024, salaries present, no Total rows</done>
</task>

<task type="auto">
  <name>Task 3: Integrate parse_regional into refresh_data.py</name>
  <files>data/pipeline/refresh_data.py</files>
  <action>
Add optional `--rapport-pdf` argument to `refresh_data.py` that triggers the regional pipeline.

1. **Add argparse argument**: `--rapport-pdf` with `help="Chemin vers le rapport annuel PDF pour extraction des donnees regionales"`. Not required. Default None.

2. **Import conditionally**: At the top of the regional section (after existing AT/MP/Trajet processing), add:
   ```python
   if args.rapport_pdf:
       from parse_regional import parse_regional_pdf
       ...
   ```

3. **Call parse_regional_pdf**: Pass `Path(args.rapport_pdf)` as input. Write output to `OUTPUT_DIR / "regional-data.json"` using `json.dump(data, f, ensure_ascii=False, indent=2)`.

4. **Print summary**: After writing, print the number of caisses extracted, matching the existing refresh_data.py print style (French, no emoji).

5. **Handle errors gracefully**: Wrap the call in try/except. On failure, print an error message to stderr and continue (do not abort the rest of refresh_data.py). This matches the pattern from existing `--pdf-dir` handling where PDF processing is optional.

Follow the existing patterns in refresh_data.py:
- Same import style (pathlib.Path, json)
- Same PIPELINE_DIR / OUTPUT_DIR convention
- Same print messaging style
  </action>
  <verify>
    <automated>cd /Users/encarv/.claude/datagouv && python data/pipeline/refresh_data.py --help 2>&1 | grep -q "rapport-pdf" && echo "OK: --rapport-pdf argument present" || echo "FAIL: missing --rapport-pdf"</automated>
  </verify>
  <done>refresh_data.py accepts --rapport-pdf, imports parse_regional conditionally, writes regional-data.json, handles errors gracefully without aborting other pipeline steps</done>
</task>

</tasks>

<verification>
1. `data/regional-data.json` exists and is valid JSON
2. JSON contains 16+ metropolitan caisses with AT, Trajet, and salaries data
3. No "Total" or "Ensemble" entries in the JSON
4. `python data/pipeline/refresh_data.py --help` shows `--rapport-pdf` argument
5. All unit tests in `test_parse_regional.py` still pass
</verification>

<success_criteria>
- regional-data.json is committed with real data from the rapport annuel PDF
- All 4 PIPE requirements are satisfied: AT extraction (PIPE-05), Trajet extraction (PIPE-06), multi-line handling (PIPE-07), salaries included (PIPE-08)
- refresh_data.py integrates the regional pipeline as an optional step
</success_criteria>

<output>
After completion, create `.planning/phases/06-pipeline-regional/06-02-SUMMARY.md`
</output>
