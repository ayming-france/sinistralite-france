---
phase: 07-structure-svg
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - js/map.js
autonomous: false
requirements:
  - MAP-01
  - MAP-04
  - MAP-11

must_haves:
  truths:
    - "La console du navigateur ne montre aucune erreur liée à map.js au chargement"
    - "document.querySelectorAll('[data-caisse]') retourne 16 éléments par vue dans DevTools"
    - "La carte n'apparait ni dans la vue MP ni comme bouton de navigation"
    - "La carte est visible sur desktop et sur mobile (375px) sans scroll horizontal"
  artifacts:
    - path: "js/map.js"
      provides: "Stub Phase 8 avec vérification data-caisse au démarrage"
      contains: "map.js"
    - path: "index.html"
      provides: "Script map.js chargé dans la page"
      contains: "map.js"
  key_links:
    - from: "js/map.js"
      to: "[data-caisse] dans le DOM"
      via: "document.querySelectorAll au DOMContentLoaded"
      pattern: "querySelectorAll.*data-caisse"
---

<objective>
Créer le stub js/map.js, le connecter à index.html, vérifier que les 16 data-caisse sont présents dans le DOM via console.log, puis obtenir une validation visuelle humaine de la carte dans le navigateur.

Purpose: Le stub amorce la convention Phase 8 (map.js = module de coloration). La vérification humaine confirme que la carte est réellement visible à l'écran et que le responsive fonctionne.
Output: js/map.js stub + checkpoint humain visuel.
</objective>

<execution_context>
@/Users/encarv/.claude/get-shit-done/workflows/execute-plan.md
@/Users/encarv/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-structure-svg/07-01-SUMMARY.md
@index.html
</context>

<tasks>

<task type="auto">
  <name>Tâche 1: Créer js/map.js stub et le référencer dans index.html</name>
  <files>js/map.js, index.html</files>
  <action>
Créer `js/map.js` comme stub ES module destiné à la Phase 8 (coloration choroplèthe). En Phase 7, ce stub fait uniquement une vérification de présence des data-caisse et log en console.

Contenu de js/map.js:

```javascript
/**
 * map.js — Phase 8: Coloration choroplèthe (stub Phase 7)
 * Vérifie la présence des 16 groupes data-caisse au démarrage.
 */

const CAISSE_IDS = [
  'alsace-moselle', 'aquitaine', 'auvergne', 'bourgogne-franche-comte',
  'bretagne', 'centre-ouest', 'centre-val-de-loire', 'cramif',
  'languedoc-roussillon', 'midi-pyrenees', 'nord-est', 'nord-picardie',
  'normandie', 'pays-de-la-loire', 'rhone-alpes', 'sud-est'
];

/**
 * Vérifie que chaque caisse ID a au moins un élément [data-caisse] dans le DOM.
 * Utilisé en Phase 7 pour confirmer la structure SVG.
 * En Phase 8, cette fonction sera étendue pour appliquer la coloration.
 */
function verifierStructureSVG() {
  let ok = 0;
  let manquants = [];

  CAISSE_IDS.forEach(id => {
    const els = document.querySelectorAll(`[data-caisse="${id}"]`);
    if (els.length > 0) {
      ok++;
    } else {
      manquants.push(id);
    }
  });

  if (manquants.length === 0) {
    console.log(`[map.js] SVG Phase 7: ${ok}/16 caisses présentes dans le DOM.`);
  } else {
    console.warn('[map.js] Caisses manquantes dans le SVG:', manquants);
  }
}

// Placeholder Phase 8: sera rempli avec la logique de coloration
export function colorierCarte(_viewType, _year, _data) {
  // TODO Phase 8: appliquer fill sur chaque [data-caisse] selon les données régionales
}

document.addEventListener('DOMContentLoaded', verifierStructureSVG);
```

Ensuite, modifier index.html pour ajouter le script map.js. L'ajouter avant la fermeture `</body>` ou juste avant le dernier script existant, en type="module":

```html
<script type="module" src="js/map.js"></script>
```

Vérifier que le pattern est cohérent avec les autres scripts du projet (si les autres scripts utilisent `type="module"`, maintenir la cohérence; sinon ajouter `defer` et retirer `type="module"`). Inspecter les balises `<script>` existantes dans index.html avant de décider.

Ne pas modifier la logique des scripts existants (app.js, nav.js, etc.).
  </action>
  <verify>
    <automated>cd /Users/encarv/.claude/datagouv && test -f js/map.js && grep -q 'colorierCarte' js/map.js && grep -q 'map\.js' index.html && echo "OK: map.js créé et référencé"</automated>
    <manual>Ouvrir DevTools Console: chercher le message "[map.js] SVG Phase 7:"</manual>
  </verify>
  <done>js/map.js existe avec CAISSE_IDS, verifierStructureSVG(), et export colorierCarte(). index.html référence map.js via balise script.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Validation visuelle: carte SVG visible, responsive, 16 caisses présentes</name>
  <action>
Validation humaine de la carte dans le navigateur. Lancer un serveur local si nécessaire:
`python3 -m http.server 8080` depuis le répertoire projet, puis visiter http://localhost:8080.

Étapes de vérification:
1. Vue AT (active par défaut): carte visible en bas du contenu, en gris neutre.
2. Vue Trajet: même carte visible.
3. Vue MP: aucune carte.
4. Navigation: 3 onglets uniquement (AT, MP, Trajet), pas de 4e.
5. Responsive 375px (DevTools > iPhone SE): pas de scrollbar horizontale.
6. Console: "[map.js] SVG Phase 7: 16/16 caisses présentes dans le DOM."

Si problèmes: carte noire = fill CSS manquant, scroll horizontal = overflow:hidden manquant sur .map-wrap, caisses manquantes = noter les IDs.
  </action>
  <verify>
    <automated>cd /Users/encarv/.claude/datagouv && grep -q 'at-mapSection' index.html && grep -q 'trajet-mapSection' index.html && ! grep -q 'mp-mapSection' index.html && echo "OK: sections carte présentes (AT+Trajet uniquement)"</automated>
    <manual>Confirmation visuelle dans le navigateur requise avant de continuer vers Phase 8.</manual>
  </verify>
  <done>Utilisateur confirme: carte visible en gris (AT + Trajet), 16/16 caisses en console, aucune scrollbar à 375px.</done>
  <what-built>
    Plan 07-01: Carte SVG France métropolitaine dans les vues AT et Trajet (16 groupes data-caisse, map.css responsive).
    Plan 07-02 Tâche 1: js/map.js stub qui vérifie et log les 16 caisses au DOMContentLoaded.
  </what-built>
  <how-to-verify>
    1. Ouvrir index.html dans le navigateur (ou `python3 -m http.server 8080` depuis le répertoire projet puis visiter http://localhost:8080).
    2. Vérifier que la carte apparait dans la vue "Accidents du Travail" (onglet AT actif par défaut) en bas du contenu, sous les graphiques existants.
    3. Passer à l'onglet "Trajet": la même carte doit être visible.
    4. Passer à l'onglet "Maladies Pro.": aucune carte ne doit être présente dans cette vue.
    5. Vérifier que les 3 boutons de navigation (AT, MP, Trajet) sont inchangés: pas de 4e onglet "Carte".
    6. Redimensionner la fenêtre à 375px de large (ou utiliser DevTools > Responsive > iPhone SE): aucune scrollbar horizontale ne doit apparaitre.
    7. Ouvrir DevTools Console: vérifier "[map.js] SVG Phase 7: 16/16 caisses présentes dans le DOM." (ou noter les caisses manquantes).
    8. Les zones de la carte doivent apparaitre en gris neutre (couleur --border-light), pas en noir.

    Problèmes attendus possibles:
    - Si la carte apparait en noir: le fill de map.css n'est pas appliqué. Vérifier que link map.css est dans le head.
    - Si scroll horizontal à 375px: vérifier overflow:hidden sur .map-wrap dans map.css.
    - Si moins de 16 caisses loguées: noter lesquelles manquent pour correction ciblée.
  </how-to-verify>
  <resume-signal>Répondre "approuvé" si tout est correct, ou décrire les problèmes observés (ex: "carte noire sur mobile", "caisse nord-est manquante")</resume-signal>
</task>

</tasks>

<verification>
Checkpoint humain: vérification visuelle dans navigateur + DevTools console.

Critères de succès du checkpoint:
- Carte visible dans AT et Trajet (pas MP, pas 4e onglet)
- Couleur gris neutre (pas noir hérité)
- Aucune scrollbar horizontale à 375px
- Console: 16/16 caisses présentes (ou liste explicite des manquants)
</verification>

<success_criteria>
- js/map.js existe avec stub colorierCarte() exporté et vérification DOMContentLoaded
- index.html référence map.js
- Validation humaine: carte visible, responsive, gris neutre, 16 caisses en console
</success_criteria>

<output>
Après complétion, créer `.planning/phases/07-structure-svg/07-02-SUMMARY.md`
</output>
